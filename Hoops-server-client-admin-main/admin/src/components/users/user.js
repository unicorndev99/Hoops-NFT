import React from 'react';
import UserContainer from '../../containers/user_container';
const User = () => {
    return (
        <div>
            user container
        </div>
    );
};

export default User;